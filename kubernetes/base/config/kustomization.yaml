apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - reconhawx-version.yaml
  - service-config.yaml
  - resource-profiles.yaml

labels:
- includeSelectors: true
  pairs:
    app: reconhawx