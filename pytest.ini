[tool:pytest]
testpaths = src/api/tests src/runner/app/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    --tb=short
    -v
markers =
    asyncio: marks tests as async (using pytest-asyncio)
