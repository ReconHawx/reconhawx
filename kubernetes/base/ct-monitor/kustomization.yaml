apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: ct-monitor

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: ct-monitor
      app.kubernetes.io/component: monitoring
      app.kubernetes.io/part-of: recon-platform

