apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: ct-monitor

resources:
  - serviceaccount.yaml
  - ct-monitor-rbac.yaml
  - deployment.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: ct-monitor
      app.kubernetes.io/part-of: reconhawx

