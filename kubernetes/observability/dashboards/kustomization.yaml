# Grafana chart sidecar watches ConfigMaps in monitoring with this label.
# See: https://github.com/grafana/helm-charts/tree/main/charts/grafana
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"

configMapGenerator:
  - name: reconhawx-grafana-dashboard-workflow-logs
    files:
      - workflow-logs.json
  - name: reconhawx-grafana-dashboard-api-logs
    files:
      - api-logs.json
  - name: reconhawx-grafana-dashboard-services-logs
    files:
      - services-logs.json
  - name: reconhawx-grafana-dashboard-frontend-logs
    files:
      - frontend-logs.json
