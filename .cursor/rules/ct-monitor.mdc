---
globs: ["src/ct-monitor/app/*.py", "src/ct-monitor/**/*.py", "src/ct-monitor/tests/**/*.py", "kubernetes/**/ct-monitor/**", "kubernetes/**/certstream-server/**", "src/certstream-server/**"]
---

# CT Monitor Service Architecture

## Overview

The CT Monitor Service detects typosquatting from newly issued SSL/TLS certificates. It consumes an aggregated certificate stream from **self-hosted [certstream-server](https://github.com/CaliDog/certstream-server)** via [certstream-python](https://github.com/CaliDog/certstream-python), matches domains against protected programs, and publishes alerts to NATS.

## Core Architecture

```
certstream-server (kubernetes/base/certstream-server)
    ↓ WebSocket ws://certstream:4000/
CertStreamConsumer (certstream_consumer.py) — certstream-python in a thread + asyncio queue
    ↓ CertificateInfo
CTMonitorService (main.py)
├── DnstwistVariationGenerator — PRIMARY: O(1) variation lookup
├── protected_domain_similarity.py — keywords + API-aligned similarity
└── CTAlertPublisher — NATS JetStream
```

## Infrastructure

| Component | Path | Notes |
|-----------|------|--------|
| certstream-server | [`kubernetes/base/certstream-server/`](kubernetes/base/certstream-server/) | **0 replicas by default**; ct-monitor scales to **1** when any program has `ct_monitoring_enabled`, back to **0** when none |
| ct-monitor | [`kubernetes/base/ct-monitor/`](kubernetes/base/ct-monitor/) | `CERTSTREAM_URL` from `service-config`; patches `deployments/scale` via [`certstream_k8s.py`](../../src/ct-monitor/app/certstream_k8s.py) + [`ct-monitor-rbac.yaml`](../../kubernetes/base/ct-monitor/ct-monitor-rbac.yaml) |
| Image | `ghcr.io/.../certstream-server:1.6.0` | **Independent** of reconhawx `APP_VERSION` — see [`third-party/certstream-server/VERSION`](../../third-party/certstream-server/VERSION) |

## Component Responsibilities

### CTMonitorService (`main.py`)
- Orchestrates lifecycle, HTTP control API, stats
- **CertStream ingestion** only when `≥1` program has `ct_monitoring_enabled` (`_reconcile_ct_ingestion` every 2s)
- **certstream-server replicas** scaled 0/1 in the same reconcile loop (scale up → HTTP ready poll → start consumer; scale down after stop consumer)
- **Pod auto-start**: lifespan probes API then `start()`; set `CT_MONITOR_AUTO_START=false` to idle until `POST /start`
- Program config reload: API `sync_ct_monitor_program_config()` → `POST /refresh-domains`

### CertStreamConsumer (`certstream_consumer.py`)
- **Primary** ingress: `certstream.listen_for_events` pattern via `CertStreamClient` in a daemon thread
- Bridges messages into asyncio; optional TLD filter at enqueue (off by default — all TLDs matched)
- Reconnects on disconnect (`CT_RECONNECT_DELAY`, default 5s)
- Default URL: `ws://certstream:4000/` (`CERTSTREAM_URL`)

### Configuration sources

| What | Where |
|------|--------|
| CertStream URL | `CERTSTREAM_URL` / ConfigMap `certstream.url` |
| Global stats interval | `system_settings.ct_monitor_runtime`; API internal + admin routes |
| Per-program similarity | `programs.ct_monitor_program_settings.similarity_threshold` on `GET /programs/{name}` |

### DnstwistVariationGenerator / `protected_domain_similarity.py` / `CTAlertPublisher`
- Unchanged: see previous patterns in this file’s matching and alert sections below.

## Data Flow

```
certstream-server (all CT logs)
    ↓
CertStreamConsumer (all TLDs by default; optional CT_INGESTION_TLD_FILTER_ENABLED)
    ↓
CTMonitorService._on_certificate()
    → variation match → keyword/similarity → NATS events.typosquat.ct_alert
    ↓
event-handler
```

## Configuration (`config.py`)

**Required:** `API_URL`, `NATS_URL`, `INTERNAL_SERVICE_API_KEY`

**CertStream:** `CERTSTREAM_URL` (default `ws://certstream:4000/`), `CT_RECONNECT_DELAY`

**K8s scaler:** `CT_CERTSTREAM_SCALE_ENABLED` (`true` in cluster; `false` for local dev), `CERTSTREAM_DEPLOYMENT_NAME` (default `certstream-server`), `KUBERNETES_NAMESPACE`, `CT_CERTSTREAM_READY_TIMEOUT` (default 90s). Auto-enables when ServiceAccount token is present unless explicitly off.

**CertStream consumer:** `CERTSTREAM_QUEUE_MAXSIZE` (default 5000), `CERTSTREAM_YIELD_EVERY_N` (default 50), `CERTSTREAM_QUEUE_DROP_WATERMARK` (default 0.8 — drop when queue fill ≥ this ratio).

**Matching throughput:** `CT_MATCH_CONCURRENCY` (default `min(4, cpu_count)` — parallel `match_certificate_sync` slots; raise with CPU limits on the Deployment).

**Ingestion TLD filter:** `CT_INGESTION_TLD_FILTER_ENABLED` (default `false` — all certificate TLDs). When `true`, uses per-program `tld_filter` union.

**Variations:** unlimited dnstwist permutations per protected domain (no cap unless `max_variations` passed explicitly)

**Runtime:** `CT_STATS_INTERVAL` (overridable via `ct_monitor_runtime` in DB)

**HTTP:** `CT_MONITOR_HTTP_HOST`, `CT_MONITOR_HTTP_PORT` (default 8002)

## Protected Domains Loading

`GET /programs`, `GET /programs/{name}` for `ct_monitoring_enabled` programs; rebuild variations on each reload. Triggers: ct-monitor `start()`, and API `sync_ct_monitor_program_config()` → `POST /refresh-domains` when CT-related program settings change (no periodic polling).

## Matching Algorithm

Two stages; skip legitimate subdomains of protected FQDNs and exact protected FQDN hits.

**Stage 1 — dnstwist variations (O(1)):** Pre-computed variation **registrable hostname labels** (public suffix stripped via `tldextract` in `domain_labels.hostname_without_public_suffix`). Certificate SANs match when their label equals a variation label **exactly** — any TLD (`d0main.live` matches variation from `d0main.com`; `my-d0main.net` does not).

**Stage 2 — keywords + similarity:** Protected subdomain **keywords** still use substring match on the full hostname (`production` in `production-d0main.live`). Fuzzy match uses `best_similarity_typo_to_protected` with apex extraction via `tldextract` (`domain_labels.extract_apex_domain` in ct-monitor; keep in sync with API `protected_domain_similarity_service.py`).

## Alert Publishing

Unchanged: NATS `events.typosquat.ct_alert`, priority from match type/score.

## HTTP Control API

- `GET /status` — full stats (admin UI); not used for Kubernetes probes
- `GET /health` — lightweight; **liveness and readiness** probes use this path only
- `POST /start`, `POST /stop`, `POST /refresh-domains`, `POST /reload-runtime-settings`

## Development Guidelines

### Performance
- **Ingestion TLD filter is off by default** — expect high CertStream volume and large variation maps; use queue backpressure and adequate memory
- When `CT_INGESTION_TLD_FILTER_ENABLED=true`, frames can be filtered **before** the asyncio queue
- Protected-domain reload builds dnstwist variations via `asyncio.to_thread`; certificate matching uses `certificate_matcher.match_certificate_sync` off the event loop in **bounded parallel** tasks (`CT_MATCH_CONCURRENCY`)
- Similarity short-circuit: `similarity_impossible_by_length` skips Levenshtein when edit distance cannot reach per-program `similarity_threshold` (safe; no false negatives)
- Backpressure: queue drops when fill ≥ `CERTSTREAM_QUEUE_DROP_WATERMARK` (default 80%); `/status` exposes `offered_per_second`, `processed_per_second`, `drop_rate`, `match_in_flight`, `similarity_skipped`
- Tune `CERTSTREAM_QUEUE_MAXSIZE`, `CERTSTREAM_YIELD_EVERY_N`, `CT_MATCH_CONCURRENCY`, and ct-monitor CPU (e.g. 4 cores in base Deployment)
- certstream-server load-sheds slow clients; keep ct-monitor queue healthy

### Error handling
- CertStream thread reconnects automatically
- NATS publish retries via publisher

### Testing
- Unit tests: `test_certstream_consumer.py`, `test_certstream_k8s.py` (mock K8s/HTTP, no cluster)
- Monitor `/status` for `total_received` / `processed` when ingestion active

## Key Dependencies

- `certstream` (certstream-python)
- `dnstwist`, `nats-py`, `aiohttp`, `fastapi`, `uvicorn`, `redis`

## Troubleshooting

### No certificates received
- `kubectl get pods -l app=certstream-server`; `curl http://certstream:4000/example.json`
- ct-monitor `/status`: `certstream_url`, `certstream_scale_enabled`, `certstream_replicas_desired`
- `kubectl get deploy certstream-server -n reconhawx` — expect 0/0 when no CT programs, 1/1 when enabled
- At least one program with `ct_monitoring_enabled`

### No alerts
- Protected domains loaded (`/status` → `protected_domains.total > 0`)
- TLD allowlist covers target domains
- Similarity threshold not too high

### NATS failures
- `publisher.is_connected`, EVENTS stream subjects

## File Organization

```
src/ct-monitor/app/
├── main.py
├── config.py
├── certstream_consumer.py
├── variation_generator.py
├── protected_domain_similarity.py
├── alert_publisher.py
└── models.py

src/certstream-server/
├── Dockerfile
└── build.sh
```
